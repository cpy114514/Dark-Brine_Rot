# Unity collaboration workflow

When working on this Unity project, make only a small, coherent change at a time. After each change, trigger or wait for Unity to reload/recompile and inspect the Console before continuing. Do not accumulate a large batch of unverified asset, scene, shader, or script edits: it makes the Editor stall and makes regressions difficult to identify.
